[project]
name = "atividade-1-engenharia"
version = "0.1.0"
description = "Pipeline ELT com Airflow + DuckDB - Gastos Diretos do Governo Federal"
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "duckdb>=1.4.1",
    "pandas>=2.0.0",
    "pyarrow>=15.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.32.0",
    "apache-airflow>=2.8.0",
]

[dependency-groups]
dev = [
    "ydata-profiling>=4.17.0",
]
